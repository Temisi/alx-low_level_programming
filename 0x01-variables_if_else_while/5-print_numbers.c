#include <stdio.h>
/**
 * main -main block
 * Description: Print all numbers of base 10, starting from 0.
 * Return: 0
 */
int main(void)
{
	int m;

	for (m = 0; m < 10; m++)
	{
		printf("%m", m);
	}
	putchar('\n');

	return (0);
}
